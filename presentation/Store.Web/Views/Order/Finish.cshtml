@model OrderModel
@{
    ViewData["Title"] = "Замовлення оформлено";
}

<p>
    Замовлення оформлено. Незабаром воно буде зібране і передане до служби доставки.
    Очікуйте на SMS-повідомлення.

<table class="table table-sm table-striped">
    <thead class="thead-dark">
        <tr>
            <th scope="col">№</th>
            <th scope="col">Автор</th>
            <th scope="col">Назва</th>
            <th scope="col">Кількість</th>
            <th scope="col">Ціна</th>
        </tr>
    </thead>
    <tbody>
@for (int i = 0; i < Model.Items.Length; i++)
{
    <tr>
        <td class="text-right">@(i + 1)</td>
        <td>@Model.Items[i].Author</td>
        <td>@Model.Items[i].Title</td>
        <td>@Model.Items[i].Count</td>
        <td>@Model.Items[i].Price</td>
    </tr>
}
    </tbody>
    <tfoot>
        <tr>
            <td colspan="3" class="text-right">Загально</td>
            <td>@Model.TotalCount</td>
            <td>@Model.TotalPrice</td>
        </tr>
    </tfoot>
</table>

<strong>Доставка</strong>
<pre>
    @Model.DeliveryDescription
</pre>
<strong>Оплата</strong>
<pre>
    @Model.PaymentDescription
</pre>
</p>
<p>
    <a href="@Url.Action("Index", "Home")">Повернутися до магазину</a>
</p>
