@model DeliveryModel
@{
    ViewData["Title"] = "Спосіб оплати";
}

<h1>Спосіб оплати</h1>

<form action="@Url.Action("StartPayment")" method="post">
    <input type="hidden" name="id" value="@Model.OrderId" />
    <div class="form-group">
        <label for="code">Спосіб оплати</label>
        <select class="form-control" name="uniqueCode">
            @foreach (var method in Model.Methods)
            {
                <option value="@method.Key">@method.Value</option>
            }
        </select>
    </div>
    <button type="submit" class="btn btn-success">Далі</button>
</form>